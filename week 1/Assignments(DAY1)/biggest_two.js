/**
 * Purpose: Compares two numbers and identifies which one is greater.
 * Key Variables:
 * - r: The first number.
 * - t: The second number.
 * Logic Flow:
 * - Uses a ternary operator to check if 'r' is greater than 't'.
 * - Logs the appropriate message to the console based on the condition.
 */
let r = 10;
let t = 20;
r > t ? console.log("r is greater than t") : console.log("t is greater than r");
